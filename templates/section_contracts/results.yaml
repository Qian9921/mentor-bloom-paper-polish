section: results
structure: object -> observation -> scoped comparison -> explanation -> synthesis
must_include:
  - what the figure/table reports
  - why the gain matters
must_avoid:
  - benchmark report tone
